import { Mail, UserRound } from 'lucide-react'
import type { CertificateFormData } from '../../types/certificate'

type Props = { form: CertificateFormData; errors: Record<string, string>; onChange: (field: string, value: string) => void }
export default function RecipientStep({ form, errors, onChange }: Props) { const field = (key: 'name' | 'email' | 'recipientId' | 'organization', label: string, placeholder: string) => <label className="certificate-field">{label}<div className={errors[key] ? 'certificate-input invalid' : 'certificate-input'}>{key === 'name' && <UserRound size={17} />}{key === 'email' && <Mail size={17} />}<input value={form.recipient[key]} onChange={e => onChange(key, e.target.value)} placeholder={placeholder} /></div>{errors[key] && <small className="field-error">{errors[key]}</small>}</label>; return <div className="form-step"><h2>Who is receiving this certificate?</h2><p>Enter the recipient's information.</p><div className="form-grid">{field('name', 'Recipient Full Name *', 'Lim Potkolbotey')}{field('email', 'Recipient Email *', 'lim@example.com')}<div className="form-split">{field('recipientId', 'Recipient ID', 'KIT-B13-001')}{field('organization', 'Organization / Institution', 'Kirirom Institute of Technology')}</div></div><div className="form-helper"><UserRound size={17} /><span>We'll use this information to identify the certificate recipient.</span></div></div> }
